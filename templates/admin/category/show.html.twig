{% extends "admin/base_admin.html.twig" %}

{% block body %}
    <div class="row">
            <div class="col-12 mt-3 d-flex justify-content-around align-items-center">
                <h1 class="text-center py-5">{{category.name}}</h1>
                <a class="btn btn-primary" href="{{ path('tag_new',{'id': category.id }) }}">Ajouter un Tag</a>
                <a class="btn btn-primary" href="{{ path('product_new',{'id': category.id }) }}">Ajouter</a>
            </div>
    </div>
    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ category.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ category.name }}</td>
            </tr>
            <tr>
                <th>ImagePath</th>
                <td>{{ category.imagePath }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('category_index') }}">back to list</a>

    <a href="{{ path('category_edit', {'id': category.id}) }}">edit</a>

    {{ include('admin/category/_delete_form.html.twig') }}
{% endblock %}
